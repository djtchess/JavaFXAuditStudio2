package com.example.patient.migration;

import org.springframework.stereotype.Component;

/**
 * Regles de gestion et decisions metier pour Patient.
 */
@Component
public class PatientPolicy {

    /** Regle metier isEligible : decision BUSINESS detectee */
    public boolean isEligible(final Object context) {
        // TODO : implementer la regle metier
        return false;
    }

    /** Regle metier canDelete : validation BUSINESS detectee */
    public boolean canDelete(final Object context) {
        // TODO : implementer la regle metier
        return false;
    }

}
